const reducer =()=>{
    const initialState ={
    place : "Bangalore"
    }
    
    const dataReducer =(state, action)=>{
    switch(action.type){
        
    }
    }
    
    return {initialState, dataReducer}
    }
    
    export default reducer;